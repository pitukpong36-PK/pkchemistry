# ✅ เช็กลิสต์ก่อนอัปขึ้นเว็บจริง (Go-Live Checklist)

> เตือนความจำสิ่งที่ต้องทำ/ตรวจ **ก่อนเผยแพร่เว็บให้นักเรียนใช้จริง**
> อัปเดตล่าสุด: 2026-06-30

---

## 🔴 สำคัญที่สุด — ระบบสมาชิก/ข้อมูลนักเรียน (ต้องทำก่อน ไม่งั้นข้อมูลไม่รวมศูนย์)

**ปัญหาปัจจุบัน:** เว็บยังทำงานใน **"โหมดเดโม (Local)"** เพราะ `firebaseConfig` ใน `index.html` ยังเป็นค่าตัวอย่าง
(`apiKey: "YOUR_API_KEY"`, `projectId: "YOUR_PROJECT_ID"` — ค้นหา `const firebaseConfig`)

```js
const CLOUD = !/^YOUR_/.test(firebaseConfig.apiKey);  // = false → ใช้ LocalBackend
```

**ผลกระทบ:** ข้อมูลการสมัครสมาชิกถูกเก็บใน `localStorage` ของ **เครื่อง/เบราว์เซอร์แต่ละคนเท่านั้น**
→ นักเรียนสมัครจากมือถือตัวเอง **ครู/แอดมินจะไม่เห็นในแดชบอร์ด** (ยังไม่มีฐานข้อมูลกลาง)

### สิ่งที่ต้องทำเพื่อให้เก็บข้อมูลสมาชิกจริง
- [ ] สร้างโปรเจกต์ Firebase + เปิด **Authentication (Email/Password)** และ **Firestore**
- [ ] ใส่ค่าจริงใน `firebaseConfig` (apiKey, authDomain, projectId, ฯลฯ) → ระบบจะสลับเป็น **CloudBackend** อัตโนมัติ
- [ ] ตั้ง **Firestore Security Rules** ให้:
  - นักเรียนอ่าน/เขียนได้เฉพาะเอกสารของตัวเอง (`users/{uid}` ของตัวเอง)
  - อีเมลครู (ดู `TEACHER_EMAILS`) อ่านคอลเลกชัน `users` ทั้งหมดได้ (ไม่งั้นแดชบอร์ดครูจะว่าง)
- [ ] ทดสอบสมัคร–ล็อกอิน–ดูแดชบอร์ดครู จากคนละเครื่องว่าข้อมูลขึ้นจริง
- 📄 ขั้นตอนละเอียด: ดู `FIREBASE_SETUP.md`

### ที่เก็บ/วิธีตรวจข้อมูลสมาชิก (เมื่อเปิด Cloud แล้ว)
- **Firebase Console → Authentication** = รายชื่อผู้สมัครทั้งหมด (อีเมล, วันสมัคร, UID)
- **Firestore → คอลเลกชัน `users`** = โปรไฟล์ + ความก้าวหน้า `{name, email, createdAt, progress}` ต่อ 1 คน
- **ในเว็บ:** ล็อกอินด้วยอีเมลครู → เมนูผู้ใช้ → **👨‍🏫 แดชบอร์ดครู** (ตาราง + Heatmap + Export CSV)

### ผู้ที่เป็น "ครู/แอดมิน"
- กำหนดในตัวแปร `TEACHER_EMAILS` (ค้นหาใน`index.html`) — ปัจจุบัน:
  `['pitukpong36@gmail.com','pitukpong36@brw.ac.th']`
- [ ] ตรวจ/เพิ่ม–ลบ อีเมลครูให้ถูกต้องก่อนเผยแพร่

### ความปลอดภัย
- รหัสผ่าน: โหมด Cloud จัดการโดย Firebase Auth (ปลอดภัย, ดูไม่ได้) · โหมด Local เก็บเป็น hash อ่อน (`strHash`/FNV) — **เดโมเท่านั้น ห้ามใช้เก็บข้อมูลจริง**

---

## 🟡 จุดอื่นที่ควรเช็คก่อนเผยแพร่

- [ ] **ต้องเสิร์ฟผ่าน http(s) ไม่ใช่ `file://`** — เพราะ:
  - คลังสื่อโหลดจาก `data/media.json` (เปิดแบบ `file://` จะพัง)
  - Service Worker / PWA ลงทะเบียนเฉพาะเมื่อเป็น http(s) เท่านั้น
- [ ] **อัปเวอร์ชันแคชใน `sw.js`** ทุกครั้งที่อัปเดตเนื้อหา (ไม่งั้นผู้ใช้เห็นของเก่าจากแคช)
- [ ] ทดสอบบนมือถือจริง (responsive) + ทดสอบติดตั้ง PWA (Add to Home Screen)
- [ ] ตรวจ `manifest.webmanifest` (ชื่อ, ไอคอน, start_url) ให้ตรงโดเมนที่อัป
- [ ] ลิขสิทธิ์: รูปเป็น inline SVG วาดเอง, ฟอนต์ OFL — พร้อมเผยแพร่สาธารณะ (ตรวจแล้ว) ✅

## 🟢 หมายเหตุ (ไม่ต้องแก้ ปล่อยไว้ได้)
- `QUIZ_SHEET_URL = ''` ตั้งใจให้ว่าง — ใช้คลังโจทย์ฝังในเว็บ (การ import จาก Google Sheets เคยทำข้อมูลเพี้ยน)

---

## 📌 ลำดับแนะนำเวลาจะอัปจริง
1. ตั้งค่า Firebase + Rules (ส่วน 🔴)
2. ทดสอบสมัคร/ล็อกอิน/แดชบอร์ดครู จาก 2 เครื่อง
3. อัปเวอร์ชัน `sw.js`
4. อัปขึ้น host ที่เป็น https (เช่น GitHub Pages / Firebase Hosting)
5. ทดสอบบนมือถือจริง + ติดตั้ง PWA
