# 2026-06-23 · เชื่อมปุ่มแล็บโมลในหน้า c2b4

## สิ่งที่พบ
- แล็บโมล (`id:'mole'`) **มีอยู่จริงและสมบูรณ์แล้ว** ใน `LABS` + `Lab.open('mole')` + `Lab.renderMole()`
  (โหมดอิสระ/ภารกิจ, SVG แอนิเมชันโมล↔มวล↔อนุภาค, ปริมาตร STP, ร้อยละองค์ประกอบ)
- ที่ขาดจริง: **หน้า c2b4 "การประยุกต์ใช้แนวคิดโมล" ไม่มีปุ่มลิงก์ไปแล็บ**
  (ตอนเพิ่ม app section บท c2b4 เมื่อ 2026-06-21 แล็บโมลยังไม่พร้อม เลยข้ามปุ่มไว้)

## สิ่งที่ทำ
- เพิ่มปุ่ม `app-btn` ใต้ tip-box ของ c2b4 (หลังบรรทัด ~5582):
  `onclick="showPage('lab');Lab.open('mole');"` → "🔢 ลองแล็บแปลงโมล ↔ มวล ↔ อนุภาค"
- ตรงแพตเทิร์นเดียวกับปุ่มแล็บบทอื่น (เช่น c2b5→dilute)

## ผลรวม
- ปุ่มลิงก์แล็บครบ 11 แล็บที่ควรมีปุ่มในหน้าเนื้อหา:
  vsepr, mole, dilute, stoich, gas, rate, equil, titrate, electro, organic, polymer
  (safety/atom/enviro เข้าผ่านหน้าแล็บโดยตรง)

## ทดสอบ
- ปุ่ม mole = 1 อัน · ปิดแท็กถูก · `<button>` balance 217/217 · HTTP 200
- หน้า `page-lab` มีจริง (บรรทัด ~1928) — `showPage('lab')` ใช้ได้

## หมายเหตุ
- ไม่แตะ JS (เพิ่ม HTML ปุ่มล้วน) → ไม่มี regression กับลอจิกแล็บ/เกม
- backup: ใช้ git (ไม่สร้าง .bak ตามแนวทางใหม่)
