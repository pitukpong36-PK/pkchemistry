# แผนพัฒนาเว็บไซต์ PKchemistry ให้ทันสมัย

จัดทำ: 19 ก.ค. 2026 — อิงสภาพจริงของโปรเจกต์ ณ วันนี้

## สภาพปัจจุบัน (จุดตั้งต้น)

| ด้าน | สถานะ |
|---|---|
| สถาปัตยกรรม | ไฟล์เดียว `index.html` ~2.8 MB (~24,000 บรรทัด) รวม HTML+CSS+JS+โจทย์ 646 ข้อ |
| PWA | มี `sw.js` (network-first) + `manifest.webmanifest` แล้ว |
| Deploy | ยังไม่มี remote git / ยังไม่ขึ้น GitHub Pages |
| สมาชิก/ข้อมูล | Firebase ยังโหมดเดโม (`firebaseConfig = YOUR_*`) เก็บผ่าน localStorage |
| เนื้อหา | ครบ 14 บท ตรวจความถูกต้องแล้ว, ลิขสิทธิ์สะอาด (SVG วาดเอง) |
| ฟีเจอร์เด่น | แบบทดสอบ, เกม RPG, มินิเกม, ห้องแล็บจำลอง, เครื่องมือเคมี, อนิเมชัน SMIL |

จุดแข็งคือเนื้อหาครบและฟีเจอร์เยอะ — สิ่งที่ "ไม่ทันสมัย" คือ **ยังเข้าถึงไม่ได้จากอินเทอร์เน็ต, โหลดช้าเพราะไฟล์ใหญ่, และ UX บางจุดยังไม่เท่ามาตรฐานเว็บปี 2026**

---

## เฟส 1 — ขึ้นออนไลน์จริง (สำคัญสุด, ~1 สัปดาห์)

เว็บที่ทันสมัยที่สุดก็ไร้ค่าถ้านักเรียนเปิดไม่ได้ เฟสนี้ทำก่อนทุกอย่าง

1. **สร้าง GitHub repo + เปิด GitHub Pages** — ได้ URL จริง `https://<user>.github.io/pkchemistry/` ฟรี, HTTPS อัตโนมัติ (จำเป็นต่อ PWA/ติดตั้งลงมือถือ)
2. **ต่อ Firebase จริง** ตาม `FIREBASE_SETUP.md` + ตั้ง Security Rules ก่อนเปิดรับสมาชิก (ตาม `DEPLOY_CHECKLIST.md`)
3. **โดเมนสั้นจำง่าย (ทางเลือก)** — เช่น `pkchemistry.com` ปีละ ~400–600 บาท ผูกกับ Pages ได้เลย
4. **QR code + ลิงก์แจกนักเรียน** — วัดผลจริงว่านักเรียนใช้แล้วติดตรงไหน ก่อนลงแรงเฟสถัดไป

ผลลัพธ์: นักเรียนเปิดจากมือถือได้ ติดตั้งเป็นแอปได้ ใช้ออฟไลน์ได้

## เฟส 2 — เร็วขึ้น: แตกไฟล์ยักษ์ (~2 สัปดาห์)

ไฟล์ 2.8 MB ทำให้เปิดครั้งแรกบนมือถือ/เน็ตช้าใช้เวลาหลายวินาที เป้าหมาย: **หน้าแรกโหลด < 1 MB**

1. **ย้ายคลังโจทย์ออกเป็น `data/quiz-bank.json`** — โจทย์ 646 ข้อ + ข้อสอบเสริมกินพื้นที่มาก โหลดแบบ lazy ตอนผู้ใช้กดเข้าแบบทดสอบ (มี pattern อยู่แล้วจาก `data/media.json`)
2. **แยก CSS และ JS ออกจาก index.html** เป็น `css/main.css`, `js/app.js`, `js/games.js`, `js/labs.js` ฯลฯ — เบราว์เซอร์แคชแยกไฟล์ได้ อัปเดตเนื้อหาไม่ต้องดาวน์โหลด JS ใหม่ทั้งก้อน
   - ⚠️ ระวังบั๊กเดิม: ตัวแปรข้าม classic `<script>` กับ `<script type=module>` ไม่ถึงกัน — ต้องทดสอบในเบราว์เซอร์จริงทุกครั้ง
3. **แยกเกม RPG เป็นหน้า/ไฟล์ของตัวเอง** — โหลดเฉพาะเมื่อกดเล่น
4. **Self-host ฟอนต์** (ดาวน์โหลด woff2 จาก Google Fonts มาไว้ในโปรเจกต์) — เร็วขึ้น + ออฟไลน์ได้สมบูรณ์ + ไม่พึ่ง third-party
5. **อัปเดต `sw.js`** ให้ precache ไฟล์ที่แยกใหม่ทั้งหมด
6. **วัดผลด้วย Lighthouse** — เป้า Performance ≥ 90 บนมือถือ

หมายเหตุ: เครื่องนี้ไม่มี Node/บันเดลเลอร์ — ใช้แนวทาง "แยกไฟล์ธรรมดา ไม่มี build step" ซึ่งเข้ากับ GitHub Pages พอดี

## เฟส 3 — หน้าตา/UX ยุค 2026 (~2–3 สัปดาห์)

1. **โหมดมืด (Dark mode)** — ตาม `prefers-color-scheme` + ปุ่มสลับ จำค่าใน localStorage (นักเรียนอ่านกลางคืนเยอะ)
2. **ปรับ mobile-first จริงจัง** — นักเรียนเกือบทั้งหมดใช้มือถือ: ปุ่ม/แท็บใหญ่พอแตะ (≥44px), bottom navigation บนจอแคบ, ตาราง/ตารางธาตุเลื่อนแนวนอนลื่น ๆ
3. **Skeleton loading + transition** — ตอนสลับบท/โหลดข้อมูลให้มีสถานะกำลังโหลด ไม่ใช่จอค้าง; ใช้ View Transitions API (มี fallback)
4. **Accessibility** — ARIA labels, contrast ≥ 4.5:1, focus ring, `prefers-reduced-motion` ปิดอนิเมชัน SMIL ได้
5. **ค้นหาทั้งเว็บ** — ช่องค้นหาเดียวหาได้ทั้งเนื้อหาบท/โจทย์/เครื่องมือ/สื่อ (ทำใน JS ล้วนได้เพราะข้อมูลอยู่ครบ)
6. **หน้า Landing แรกให้ "ขาย" ได้** — hero ชัดว่าเว็บนี้คืออะไร, ปุ่มเข้าเรียนเด่น, สถิติ (14 บท / 646 ข้อ / เกม) โชว์ทันที

## เฟส 4 — ฟีเจอร์การเรียนรู้สมัยใหม่ (~3–4 สัปดาห์)

1. **แดชบอร์ดความก้าวหน้า** — กราฟคะแนนรายบท, จุดอ่อน/จุดแข็ง, streak รายวัน (ข้อมูลมีอยู่แล้วใน progress)
2. **ทบทวนแบบ Spaced Repetition** — เก็บข้อที่ตอบผิด แล้วสุ่มวนกลับมาถามซ้ำตามช่วงเวลา (ฟีเจอร์เด่นที่แอปเรียนดัง ๆ ใช้กัน)
3. **โหมดครู** — แดชบอร์ดดูภาพรวมห้อง (ต้องมี Firebase จริงจากเฟส 1), export คะแนน CSV
4. **แชร์ผลสอบ/ป้ายรางวัล** — การ์ดผลคะแนนเป็นรูป แชร์ลง LINE/IG ได้ (Web Share API) — เป็น marketing ฟรี
5. **แจ้งเตือนอ่านหนังสือ** (ทางเลือก) — Web Push ผ่าน Firebase Messaging

## เฟส 5 — ระบบงานเบื้องหลังยั่งยืน (ต่อเนื่อง)

1. **GitHub Actions ตรวจอัตโนมัติ** ทุก push: HTML validate, ลิงก์เสีย, ขนาดไฟล์เกินงบ (budget check)
2. **เวิร์กโฟลว์อัปเดตโจทย์** — ตัดสินใจทางเดียว: โจทย์ฝังใน JSON เป็น source of truth (Google Sheets เคยทำข้อมูลเพี้ยน — ใช้เป็นช่องทางร่างเท่านั้น แล้ว export เข้ามา)
3. **เวอร์ชัน + changelog ในหน้าเว็บ** — นักเรียนเห็นว่าอัปเดตอะไรใหม่
4. **สำรองข้อมูล** — Firestore export อัตโนมัติรายเดือน

---

## ลำดับความสำคัญสรุป

```
เฟส 1 ขึ้นออนไลน์  ──ต้องทำก่อน──▶  เฟส 2 เร็วขึ้น  ──▶  เฟส 3 UX
                                                          │
                        เฟส 4 ฟีเจอร์ใหม่ ◀───────────────┘
                        เฟส 5 ทำคู่ขนานได้ตั้งแต่มี repo บน GitHub
```

สิ่งที่ **ไม่แนะนำ** ให้ทำ (ดูทันสมัยแต่ไม่คุ้ม):
- ย้ายไป React/Vue/framework — ต้องเขียนใหม่เกือบหมด เสี่ยงพังของที่ตรวจความถูกต้องไว้แล้ว โดยนักเรียนไม่ได้อะไรเพิ่ม
- เพิ่ม build tooling (Vite/webpack) — เครื่องไม่มี Node และ GitHub Pages เสิร์ฟไฟล์ธรรมดาได้ดีอยู่แล้ว
- AI chatbot ตอบคำถามเคมี — ค่าใช้จ่าย API ต่อเนื่อง + เสี่ยงตอบผิดในวิชาที่ตรวจเนื้อหาไว้เข้มงวด
